GEMINI_API_KEY=your-key-here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vectordb
DB_USER=postgres
DB_PASSWORD=postgres
JWT_SECRET_KEY=<generate with: openssl rand -hex 32>
JWT_EXPIRE_MINUTES=60
DEMO_USERNAME=your-username-here
DEMO_PASSWORD_HASH=<the bcrypt hash from step 2 — paste the whole thing>
